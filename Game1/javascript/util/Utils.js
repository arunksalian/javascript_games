/**
 * Created by arun on 30/6/16.
 */
var Utils = {
    random : function () {
        var number = Math.floor(Math.random() * 5) + 1;
        return number;
        //return Constants.blockType.TYPE_LINE
    }
};
